# Skill Registry

Auto-generated by sdd-init for relocation-project.

## User Skills (Available)

| Skill | Trigger |
|-------|---------|
| pytest | Python tests - fixtures, mocking, markers |

## Project Standards (Compact Rules)

### pytest
- Use `@pytest.fixture` for setup/teardown
- Prefer `pytest.raises()` for exception testing
- Use `@pytest.mark.parametrize` for table-driven tests
- Mock with `unittest.mock` or `pytest-mock`
- Keep tests focused: Arrange, Act, Assert
